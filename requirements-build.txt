# ──────────────────────────────────────────────────────────────────────────────
# JARVIS AI Platform — Build Dependencies
# ──────────────────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements-build.txt
# ──────────────────────────────────────────────────────────────────────────────

# ── Core dependencies (from main requirements.txt) ───────────────────────────────
groq>=0.9.0
edge-tts>=6.1.9
python-dotenv>=1.0.0
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0
pygame>=2.5.0
httpx>=0.27.0
keyboard>=0.13.5

# ── Build tools ──────────────────────────────────────────────────────────────────
pyinstaller>=5.13.0

# ── Optional: UPX for executable compression (smaller file size) ─────────────────
# Download from: https://upx.github.io/
# Add UPX to your PATH or place it in the same directory as the spec file
